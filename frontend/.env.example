# Used by server-side API requests. Browser requests use same-origin /api rewrites.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Target used by the Next.js API and upload rewrites.
API_URL=http://localhost:4000
